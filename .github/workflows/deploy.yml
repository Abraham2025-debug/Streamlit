name: Deploy Streamlit App

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"

      - name: Install Dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt || true
          pip install moviepy
          pip freeze  # Check installed packages

      - name: Debug MoviePy Installation
        run: python -c "import moviepy.editor as mp; print('MoviePy is installed successfully!')"

      - name: Run Streamlit App (Optional)
        run: streamlit run annotation_tool_streamlit.py --server.headless true
